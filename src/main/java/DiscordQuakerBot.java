/**
 * The Bot which interfaces with Discord. User commands will be captured and sent for processing.
 * Responses will be fielded to the user via Discord server API.
 */
public class DiscordQuakerBot {
}
